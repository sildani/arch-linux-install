#!/bin/bash

# Script for setting up Github. Edit script with your details before use.

echo "EDIT SCRIPT BEFORE USE"

# ssh-keygen -t rsa -b 4096 -C "YOUR_EMAIL"
# eval "$(ssh-agent -s)"
# ssh-add ~/.ssh/id_rsa
# git config --global user.name "YOUR_NAME"
# git config --global user.email "YOUR_EMAIL"

# echo "

# Now add ~/.ssh/id_rsa.pub to your Github account and verify with the following command:

#     ssh -T git@github.com"